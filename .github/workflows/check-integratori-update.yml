name: Monitor and Update Integratori Database

on:
  schedule:
    - cron: '0 7 * * 1'
  workflow_dispatch:

# Node.js 24 erzwingen (behebt Deprecation-Warnung)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  issues: write

jobs:
  check-and-update:
    runs-on: ubuntu-latest

    steps:

      # 1. Checkout mit explizitem Token fuer Push-Berechtigung
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      # 2. Python + pdfplumber
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install pdfplumber
        run: pip install pdfplumber --quiet

      # 3. Aktuelle Version lesen (Leerzeichen/Newlines entfernen)
      - name: Read current known version
        id: current
        run: |
          if [ -f .github/integratori-version.txt ]; then
            VERSION=$(cat .github/integratori-version.txt | tr -d '[:space:]')
          else
            VERSION="11"
          fi
          # Sicherstellen dass es eine Zahl ist
          if ! [[ "$VERSION" =~ ^[0-9]+$ ]]; then
            echo "WARNUNG: Ungueltige Version '$VERSION', setze auf 11"
            VERSION="11"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Aktuelle bekannte Version: $VERSION"

      # 4. Neue Version pruefen
      - name: Check for new PDF version
        id: check
        run: |
          set -e
          CURRENT="${{ steps.current.outputs.version }}"
          NEXT=$(( CURRENT + 1 ))
          BASE="https://www.salute.gov.it/new/sites/default/files"
          URL_AZ="${BASE}/INTEGRATORI_NOTIFICATI_ORD_AZ_${NEXT}.pdf"
          URL_PROD="${BASE}/INTEGRATORI_NOTIFICATI_ORD_PROD_${NEXT}.pdf"

          echo "Pruefe AZ   v${NEXT}: $URL_AZ"
          STATUS_AZ=$(curl -s -o /dev/null -w "%{http_code}" --head --max-time 20 "$URL_AZ" || echo "000")

          echo "Pruefe PROD v${NEXT}: $URL_PROD"
          STATUS_PROD=$(curl -s -o /dev/null -w "%{http_code}" --head --max-time 20 "$URL_PROD" || echo "000")

          echo "HTTP Status AZ:   $STATUS_AZ"
          echo "HTTP Status PROD: $STATUS_PROD"

          echo "next_version=$NEXT" >> $GITHUB_OUTPUT

          if [ "$STATUS_AZ" = "200" ]; then
            echo "found=true"       >> $GITHUB_OUTPUT
            echo "pdf_url=$URL_AZ" >> $GITHUB_OUTPUT
            echo "variant=AZ"      >> $GITHUB_OUTPUT
            echo "Neue Version gefunden: AZ v${NEXT}"
          elif [ "$STATUS_PROD" = "200" ]; then
            echo "found=true"        >> $GITHUB_OUTPUT
            echo "pdf_url=$URL_PROD" >> $GITHUB_OUTPUT
            echo "variant=PROD"      >> $GITHUB_OUTPUT
            echo "Neue Version gefunden: PROD v${NEXT}"
          else
            echo "found=false" >> $GITHUB_OUTPUT
            echo "Keine neue Version gefunden. Naechste Pruefung: naechsten Montag."
          fi

      # 5. PDF herunterladen
      - name: Download new PDF
        if: steps.check.outputs.found == 'true'
        run: |
          set -e
          URL="${{ steps.check.outputs.pdf_url }}"
          echo "Lade herunter: $URL"
          curl -L --max-time 180 --retry 3 --retry-delay 10 \
            --fail --show-error \
            "$URL" -o /tmp/integratori_new.pdf
          SIZE=$(du -sh /tmp/integratori_new.pdf | cut -f1)
          echo "Download abgeschlossen: $SIZE"
          # Minimale Groesse pruefen (PDF sollte > 1 MB sein)
          BYTES=$(stat -c%s /tmp/integratori_new.pdf)
          if [ "$BYTES" -lt 1000000 ]; then
            echo "FEHLER: Heruntergeladene Datei zu klein ($BYTES Bytes) - kein gueltiges PDF"
            exit 1
          fi

      # 6. PDF → JSON konvertieren
      - name: Convert PDF to JSON
        if: steps.check.outputs.found == 'true'
        run: |
          set -e
          echo "Starte Konvertierung..."
          python3 .github/scripts/pdf_to_json.py \
            /tmp/integratori_new.pdf \
            data/integratori.json
          echo "Konvertierung abgeschlossen."
          echo "JSON-Groesse: $(du -sh data/integratori.json | cut -f1)"

      # 7. Committen und pushen
      - name: Commit and push
        if: steps.check.outputs.found == 'true'
        run: |
          set -e
          NEXT="${{ steps.check.outputs.next_version }}"
          echo "$NEXT" > .github/integratori-version.txt

          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git status
          git add data/integratori.json .github/integratori-version.txt
          git diff --cached --stat
          git commit -m "data: Integratori-Datenbank automatisch aktualisiert auf Version ${NEXT}"
          git push origin HEAD
          echo "Push abgeschlossen. GitHub Pages deployt automatisch."

      # 8. Erfolgs-Issue
      - name: Create success issue
        if: steps.check.outputs.found == 'true' && success()
        uses: actions/github-script@v7
        with:
          script: |
            const next    = '${{ steps.check.outputs.next_version }}';
            const url     = '${{ steps.check.outputs.pdf_url }}';
            const variant = '${{ steps.check.outputs.variant }}';
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              title: `Database aggiornato automaticamente — versione ${next}`,
              body: [
                `## Database Integratori aggiornato`,
                ``,
                `| Campo        | Valore |`,
                `|--------------|--------|`,
                `| Versione     | ${next} |`,
                `| Variante PDF | ${variant === 'AZ' ? 'Ordine per Azienda' : 'Ordine per Prodotto'} |`,
                `| PDF sorgente | [Scarica](${url}) |`,
                `| Sito live    | https://giovanola.github.io/integratori/ |`,
                ``,
                `**Unica azione manuale:** aggiornare \`APP_VERSION\` e \`APP_RELEASE\` in \`index.html\`.`,
              ].join('\n'),
              labels: ['data-update'],
            });

      # 9. Fehler-Issue
      - name: Create error issue
        if: steps.check.outputs.found == 'true' && failure()
        uses: actions/github-script@v7
        with:
          script: |
            const next = '${{ steps.check.outputs.next_version }}';
            const url  = '${{ steps.check.outputs.pdf_url }}';
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              title: `ERRORE: Aggiornamento automatico fallito — versione ${next}`,
              body: [
                `## Aggiornamento automatico fallito`,
                ``,
                `Vedi il log completo: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`,
                ``,
                `**PDF:** [${url}](${url})`,
                ``,
                `**Azione:** caricare il PDF manualmente su Claude per la conversione.`,
              ].join('\n'),
              labels: ['data-update', 'bug'],
            });
